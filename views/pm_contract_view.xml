<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <record id="contract_view_form" model="ir.ui.view">
        <field name="name">contract_view_form</field>
        <field name="model">pm.contract</field>
        <field name="arch" type="xml">
            <form string="contract_form">

                <header>
                        <button name="action_confirm" string="Confirm" type="object" class="oe_highlight" invisible="state != 'draft'"/>
                        <button name="action_sign" string="Sign" type="object" class="oe_highlight" invisible="state != 'confirmed'"/>
                        <button name="action_cancel" string="Cancel" type="object" class="oe_highlight" invisible="state not in ('draft', 'confirmed')"/>
                        <button name="action_set_to_draft" string="Set to Draft" type="object" class="oe_highlight" invisible="state not in ('cancel', 'signed')"/>

                        <field name="state" widget="statusbar" statusbar_visible="draft,confirmed,signed"/>


                </header>
                <sheet>
                    <group>
                        <field name="name"/>
                        <field name="partner_id"/>
                    </group>
                    <group>
                        <field name="contract_date"/>
                        <field name="project_id"/>
                    </group>
                    <notebook>
                        <page string="Documents">
                            <field name="doc_ids">
                                <tree>
                                    <field name="name" />
                                    <field name="document" />
                                </tree>
                            </field>
                        </page>
                    </notebook>
                </sheet>
                <div class="oe_chatter">
                    <field name="message_follower_ids" widget="mail_followers"/>
                    <field name="activity_ids" widget="mail_activity"/>
                    <field name="message_ids" widget="mail_thread"/>
                </div>
            </form>
        </field>
    </record>

    <record id="contract_view_tree" model="ir.ui.view">
        <field name="name">contract_view_tree</field>
        <field name="model">pm.contract</field>
        <field name="arch" type="xml">
            <tree string="contract_tree">
                <field name="name"/>
                <field name="partner_id"/>
                <field name="state"/>
            </tree>
        </field>
    </record>

    <record id="contract_action" model="ir.actions.act_window">
        <field name="name">Real Estate Contract</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">pm.contract</field>
        <field name="view_mode">tree,form</field>
    </record>
</odoo>

